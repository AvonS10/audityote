# Normalize line endings. Keeps shell scripts (mvnw, Caddyfile) LF on every OS.
* text=auto eol=lf

# Windows-only scripts must stay CRLF
*.bat  text eol=crlf
*.cmd  text eol=crlf

# Binary assets — never normalize
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.jar  binary
